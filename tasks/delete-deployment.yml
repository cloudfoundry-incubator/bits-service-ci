---
platform: linux

image: docker:///flintstonecf/ci

inputs:
- name: git-bits-service-ci

params:
  BOSH_TARGET:
  BOSH_USERNAME:
  BOSH_PASSWORD:
  DEPLOYMENT_NAME:

run:
  path: git-bits-service-ci/scripts/delete-deployment.sh
