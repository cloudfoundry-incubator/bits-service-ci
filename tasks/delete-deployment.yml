---
platform: linux

image: docker:///flintstonecf/ci

inputs:
- name: ci-tasks

params:
  BOSH_TARGET:
  BOSH_USERNAME:
  BOSH_PASSWORD:
  DEPLOYMENT_NAME:

run:
  path: ci-tasks/scripts/delete-deployment.sh
