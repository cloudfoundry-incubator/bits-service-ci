---
platform: linux

image: docker:///flintstonecf/ci

params:
  MANIFEST_STUBS:
  IAAS: lite

inputs:
- name: git-bits-service-ci
- name: git-bits-service-release
- name: git-cf-release

outputs:
- name: manifests

run:
  path: git-bits-service-ci/scripts/update-cf-manifest.sh
