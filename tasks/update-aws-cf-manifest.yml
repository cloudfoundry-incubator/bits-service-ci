---
platform: linux

image: docker:///flintstonecf/ci

params:
  MANIFEST_STUBS:

inputs:
- name: git-bits-service-ci
- name: git-bits-service-release
- name: git-cf-release

outputs:
- name: manifests

run:
  path: git-bits-service-ci/scripts/update-aws-cf-manifest.sh
