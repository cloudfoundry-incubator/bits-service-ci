---
platform: linux

image_resource: { type: docker-image, source: { repository: flintstonecf/ci } }

inputs:
  - name: git-bits-service-ci

params:
  BOSH_TARGET:
  BOSH_USERNAME:
  BOSH_PASSWORD:
  ERRAND_NAME:
  DEPLOYMENT_NAME:

run:
  path: git-bits-service-ci/scripts/run-errand.sh
